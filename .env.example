# Environment Configuration for PrusaLink OAuth2 Proxy
# Copy this to .env and fill in your values
# All credentials are automatically processed at container startup

# ============================================
# Domain Configuration
# ============================================
# The domain users will access (e.g., printers.example.com)
DOMAIN=printers.example.com

# Dev mode: set true to disable Caddy automatic HTTPS and serve HTTP only
# Accepted truthy values: true, 1, yes
CADDY_DEV_MODE=false

# Email used for ACME / Let's Encrypt registration when HTTPS is enabled
ACME_EMAIL=admin@example.com

# Cloudflare API token for DNS challenge certificate provisioning
# Required permissions: Zone.Zone:Read and Zone.DNS:Edit
CLOUDFLARE_API_TOKEN=replace_with_cloudflare_api_token

# ============================================
# PrusaLink Configuration
# ============================================
# PrusaLink server address
PRUSALINK_URL=http://10.69.0.160/
PRUSALINK_USERNAME=maker
PRUSALINK_PASSWORD=MaWN8bvd3xHSstu

# ============================================
# Azure AD B2C Configuration
# ============================================
# Open your user flow metadata document, for example:
# https://your-tenant.b2clogin.com/your-tenant.onmicrosoft.com/B2C_1_signupsignin/v2.0/.well-known/openid-configuration
# Then copy the JSON "issuer" value into this variable.
AZURE_B2C_OIDC_ISSUER_URL=https://ps1b2c.b2clogin.com/eec9349e-dc2d-4733-bf2e-2c1d4bf06d3d/v2.0/

# OAuth application Client ID (from the Azure AD B2C app registration)
AZURE_B2C_CLIENT_ID=388e2daf-027e-4395-af31-398497b3d1a6

# OAuth application Client Secret (from the Azure AD B2C app registration)
AZURE_B2C_CLIENT_SECRET=fEv8Q~9WUOpPAMiE6r1oJoN5JZORDDxtnYVRTbiP

# Claim that contains the user's email address.
# Keep this as "email" if your user flow emits an email claim.
AZURE_B2C_EMAIL_CLAIM=email

# Optional overrides for HTTP-only local development.
# Keep defaults for production HTTPS.
# OAUTH2_PROXY_REDIRECT_URL=http://printers.example.com/oauth2/callback
# OAUTH2_PROXY_COOKIE_SECURE=false

# ============================================
# oauth2-proxy Session Configuration
# ============================================
# Generate with: openssl rand -base64 32
# Used for encrypting session cookies
COOKIE_SECRET=generate_with_openssl_rand_-base64_32
